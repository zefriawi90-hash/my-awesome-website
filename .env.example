# Konfigurasi Environment untuk Private Storage System
# Salin file ini ke .env dan ubah nilai-nilainya

# Port server (default: 3000)
PORT=3000

# JWT Secret - Gunakan string acak yang panjang (minimal 32 karakter)
# Contoh: JWT_SECRET=your-super-secret-key-here
JWT_SECRET=

# CORS Origin - Domain yang diizinkan (gunakan * untuk semua, atau domain spesifik)
# Contoh: CORS_ORIGIN=http://localhost:3000
CORS_ORIGIN=*

# Admin Password - Password untuk user admin (akan di-generate otomatis jika kosong)
# ADMIN_PASSWORD=

# Environment (development/production)
NODE_ENV=development
